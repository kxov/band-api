# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\User\Infrastructure\Controller\:
        resource: '../src/User/Infrastructure/Controller/'
        tags: [ 'controller.service_arguments' ]

    App\TodoList\Infrastructure\Controller\:
        resource: '../src/TodoList/Infrastructure/Controller/'
        tags: [ 'controller.service_arguments' ]

    App\Todo\Infrastructure\Controller\:
        resource: '../src/Todo/Infrastructure/Controller/'
        tags: [ 'controller.service_arguments' ]

    App\Band\Infrastructure\Controller\:
        resource: '../src/Band/Infrastructure/Controller/'
        tags: [ 'controller.service_arguments' ]

    App\Shared\Infrastructure\ArgumentResolver\CommandResolver:
        tags:
            - { name: controller.argument_value_resolver, priority: 99 }

    App\Shared\Infrastructure\Listener\ApiExceptionListener:
        tags:
            - { name: kernel.event_listener, event: kernel.exception, priority: 1 }

#    App\User\Domain\Model\UserRepositoryInterface: '@App\User\Infrastructure\Domain\Repository\DoctrineUserRepository'
#    App\User\Infrastructure\Domain\Repository\DoctrineUserRepository:
#        factory: [ '@doctrine', getRepository ]
#        arguments:
#            - App\User\Domain\Model\User

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones